name: Pattern Intelligence Pipeline

on:
  schedule:
    - cron: '30 14 * * *'  # Daily at 14:30 UTC
  workflow_dispatch: {}

# PIE pipeline has moved to Ai-Project repo.
# This workflow triggers the canonical pipeline there.
jobs:
  trigger-ai-project-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger PIE Full Pipeline in Ai-Project
        run: |
          echo "PIE pipeline has moved to DroneWuKong/Ai-Project"
          echo "Trigger: Actions → PIE Full Pipeline (Real Flags) → Run workflow"
          echo "Or it runs automatically daily at 14:30 UTC via pie-pipeline-full.yml"
